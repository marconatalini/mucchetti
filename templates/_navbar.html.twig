<nav class="navbar navbar-expand-lg" aria-label="navbar">
    <div class="container">
        {#                            <a href="/" class="d-flex align-items-start col-md-3 mb-2 mb-md-0 text-dark text-decoration-none">#}
        <a href="/" class="me-auto">
            <img src="{{ asset('images/logo_europrofili.svg') }}" alt="Logo europrofili">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if is_granted('ROLE_BOSS') %}
                    <li class="nav-item">
                        <a class="nav-link me-5" href="{{ path('app_permit_action_index') }}">Staff request
{#                            {{ render(path('app_permit_alert', {status: 'submitted'})) }}#}
                        </a>
                    </li>
                {% elseif is_granted("ROLE_STAFF") %}
                    <li class="nav-item">
                        <a class="nav-link me-5" href="{{ path('app_permit_action_index') }}">Staff request
{#                            {{ render(path('app_permit_alert', {status: 'approved'})) }}#}
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="{{ path('app_permit_index') }}">Richieste</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_permit_new') }}">Nuovo</a>
                    </li>
                {% endif %}
                {#<li class       ="nav-item">
                    <a class="nav-link disabled" aria-disabled="true">Disabled</a>
                </li>#}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown"
                       aria-expanded="false">
                        <i class="fa-solid fa-user"></i> {{ app.user ? app.user : 'User' }}</a>
                    <ul class="dropdown-menu">
                        {% if app.user %}
                            <li>
                                <a class="dropdown-item" href="{{ path('app_employee_edit', {id: app.user.id}) }}">I miei dati</a>
                            </li>
                            <li>
                                <a href="{{ path('app_logout') }}" class="dropdown-item">Logout</a>
                            </li>
                        {% else %}
                            <li>
                                <a class="dropdown-item" href="{{ path('app_login') }}">LogIn</a>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item nav-link disabled" aria-disabled="true">SingUp</a>
{#                                <a class="dropdown-item" href="{{ path('app_employee_new') }}">SignUp</a>#}
                            </li>
                        {% endif %}
                        {#<li><a class="dropdown-item" href="#">Another action</a></li>
                                            <li><a class="dropdown-item" href="#">Something else here</a></li>#}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

{#{% for transition in workflow_transitions(permit) %}#}
{#    {% if workflow_can(stamping, transition.name) %}#}
{#        <a href="{{ path('app_permit_action' , {id: permit.id, action: transition.name}) }}" class="btn btn-outline-primary">{{ transition.name }}</a>#}
{#    {% endif %}#}
{#{% else %}#}
{#    No actions available.#}
{#{% endfor %}#}


{% if workflow_can(stamping, 'delete') %}
    <a class="btn btn-outline-danger" href="{{ path('app_permit_action' , {action: 'delete', id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="cancella">
        <i class="fa-solid fa-trash"></i>
    </a>
{% endif %}

{% if workflow_can(stamping, 'submit') or workflow_can(stamping, 'review')%}
    <a class="animate__animated animate__heartBeat animate__infinite animate__slow btn btn-outline-success" href="{{ path('app_stamping_action' , {action: 'submit', id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="invia">
        <i class="fa-solid fa-envelope"></i>
    </a>
    <a class="btn btn-outline-secondary" href="{{ path('app_stamping_edit' , {id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="modifica">
        <i class="fa-solid fa-pencil"></i>
    </a>
{% endif %}

{% if workflow_can(stamping, 'approve') %}
    {#<a class="btn btn-outline-danger" href="{{ path('app_stamping_action' , {action: 'reject', id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="respingi">
        <i class="fa-solid fa-backward"></i>
    </a>#}
    <a class="btn btn-outline-success" href="{{ path('app_stamping_action' , {action: 'approve', id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="visto">
        <i class="fa-solid fa-check"></i>
    </a>
{% endif %}

{% if workflow_can(stamping, 'close') %}
    <a class="btn btn-outline-success" href="{{ path('app_stamping_action' , {action: 'close', id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="registra">
        <i class="fa-solid fa-check"></i>
    </a>
    <a class="btn btn-outline-secondary" href="{{ path('app_stamping_print', {id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="stampa">
        <i class="fa-solid fa-print"></i>
    </a>
{% endif %}

{% if stamping.status == 'registered' %}
    <a class="btn btn-outline-secondary" href="{{ path('app_stamping_print', {id: stamping.id}) }}"
       data-bs-toggle="tooltip" title="stampa">
        <i class="fa-solid fa-print"></i>
    </a>
{% endif %}
